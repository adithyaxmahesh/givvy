# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Session (required in production — use a long random secret, e.g. openssl rand -hex 32)
SESSION_SECRET=your-session-secret-in-production

# App URL (used in emails and redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: OpenAI (for AI matching)
OPENAI_API_KEY=

# Optional: Resend (for welcome and notification emails)
RESEND_API_KEY=
